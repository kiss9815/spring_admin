server:
  port: 8082

spring:
  application:
    name: spring-client 
  boot:
    admin:
      client:
         url: http://localhost:8989  
  
  
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS

      
         